import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MockupRoutingModule } from './mockup/mockup-routing.module';

@NgModule({
  imports: [
    CommonModule,
    MockupRoutingModule
  ]
})
export class MockupModule { }